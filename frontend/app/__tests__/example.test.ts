/**
 * Placeholder test so Jest runs successfully.
 * Add your real tests here or in other __tests__ folders.
 */
describe('Jest setup', () => {
  it('runs successfully', () => {
    expect(true).toBe(true)
  })
})
